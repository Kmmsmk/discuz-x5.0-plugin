# 需求文档

## 1. 应用概述

### 1.1 应用名称
AI 接口桥接插件（AI Bridge Plugin）+ Discuz X5.0 演示网站

### 1.2 应用描述
本应用包含两部分：
1. **aibridge 插件**：Discuz X5.0 论坛系统的扩展插件，用于集成多平台 AI 大模型能力，为论坛用户和管理员提供 AI 对话、内容辅助等功能。插件标识符为 aibridge
2. **演示网站**：基于 React + Vite 构建的完整演示环境，模拟 Discuz X5.0 论坛系统 + aibridge 插件的前后台功能，使用 Mock 数据在前端模拟所有交互，访问地址为 http://127.0.0.1:8082

### 1.3 支持的 AI 平台
- OpenAI（GPT-4o、GPT-4、GPT-3.5-turbo 等）
- 百度文心一言（ERNIE）
- 阿里通义千问（Qwen）
- 讯飞星火（Spark）
- 腾讯混元（Hunyuan）
- DeepSeek
- Anthropic Claude
- 自定义平台（Custom，兼容 OpenAI Chat Completions 格式）

## 2. 用户与使用场景

### 2.1 目标用户
- 论坛管理员：配置和管理 AI 平台接入
- 论坛普通用户：使用 AI 对话和内容辅助功能
- 演示体验用户：通过浏览器访问演示网站体验完整功能

### 2.2 核心使用场景
- 管理员在后台配置多个 AI 平台的 API 密钥和参数
- 用户在前台通过悬浮窗与 AI 进行多轮对话
- 用户在发帖时使用 AI 辅助功能润色、续写或翻译内容
- 用户查看帖子时获取 AI 智能回复建议
- 体验用户通过演示网站浏览论坛、查看帖子、使用 AI 助手、访问后台管理和 API 文档

## 3. 页面结构与功能说明

### 3.1 整体结构

```
应用根目录
├── aibridge 插件（PHP 插件包）
│   ├── 后台管理模块
│   │   ├── 平台配置管理页面
│   │   ├── 用量统计页面
│   │   └── Prompt 模板管理页面
│   └── 前台功能模块
│       ├── AI 对话悬浮窗
│       ├── 发帖编辑器 AI 辅助按钮
│       └── 帖子 AI 回复建议区域
└── 演示网站（React + Vite）
    ├── 论坛前台
    │   ├── 论坛首页
    │   ├── 板块页
    │   └── 帖子详情页
    ├── aibridge 后台管理
    │   ├── 平台配置页
    │   ├── Prompt 模板管理页
    │   └── 用量统计页
    └── API 接口文档展示页
```

### 3.2 aibridge 插件后台管理模块

#### 3.2.1 平台配置管理页面
- 展示所有支持的 AI 平台列表
- 每个平台包含以下配置项：
  - 平台名称（不可编辑）
  - 启用/禁用开关
  - API Key 输入框（加密存储）
  - API 地址输入框（自定义平台必填）
  - 模型选择下拉框
  - 额外配置参数（JSON 格式）
- 设置全局默认平台和模型
- 保存配置按钮

#### 3.2.2 用量统计页面
- 显示总调用次数
- 按平台统计调用次数和 Token 消耗
- 按用户组统计调用次数
- 时间范围筛选（今日、本周、本月、自定义）

#### 3.2.3 Prompt 模板管理页面
- 展示已创建的 Prompt 模板列表
- 新增模板功能：输入模板名称、系统 Prompt 内容
- 编辑和删除已有模板
- 模板可在前台 AI 对话中快速选用

#### 3.2.4 权限与限制设置
- 按用户组设置 AI 功能使用权限（勾选可用用户组）
- 设置每用户每日最大请求次数
- 设置每次请求最大 Token 数

### 3.3 aibridge 插件前台功能模块

#### 3.3.1 AI 对话悬浮窗
- 前台页面右下角显示悬浮按钮
- 点击按钮展开对话框
- 对话框包含：
  - 消息展示区域（显示用户消息和 AI 回复）
  - 输入框（用户输入消息）
  - 发送按钮
  - Prompt 模板选择下拉框（可选）
  - 清空对话按钮
- 支持多轮对话，保留对话历史
- 显示 AI 回复加载状态

#### 3.3.2 发帖编辑器 AI 辅助按钮
- 在发帖编辑器工具栏新增 AI 按钮
- 用户选中编辑器中的文本后点击 AI 按钮
- 弹出操作菜单：
  - 润色
  - 续写
  - 翻译
- 选择操作后，AI 处理选中内容并将结果插入编辑器

#### 3.3.3 帖子 AI 回复建议区域
- 在帖子内容末尾显示「AI 智能回复建议」区域
- 点击「生成建议」按钮，AI 根据帖子内容生成回复建议
- 显示生成的回复建议文本
- 用户可点击「使用此回复」按钮，将建议内容填入回复框

### 3.4 aibridge 插件核心 API 接口

#### 3.4.1 aibridge/chat 接口
- 接收参数：
  - 用户消息内容
  - 指定平台（可选，默认使用全局默认平台）
  - 指定模型（可选）
  - 对话历史 ID（可选，用于多轮对话）
- 处理流程：
  - 验证用户权限和频率限制
  - 调用指定平台 API
  - 记录调用日志
  - 保存对话历史
- 返回 AI 回复内容
- 支持流式输出（可选配置）

#### 3.4.2 aibridge/platforms 接口
- 返回当前已启用的 AI 平台列表
- 包含平台名称、可用模型列表

### 3.5 aibridge 插件数据表结构

#### 3.5.1 cdb_aibridge_config 表
存储各平台配置信息，字段包括：
- platform（平台标识）
- api_key（API 密钥，加密存储）
- api_url（API 地址）
- model（模型名称）
- enabled（启用状态）
- extra_conf（额外配置，JSON 格式）

#### 3.5.2 cdb_aibridge_log 表
记录 AI 调用日志，字段包括：
- uid（用户 ID）
- platform（平台标识）
- model（模型名称）
- prompt_tokens（输入 Token 数）
- completion_tokens（输出 Token 数）
- created_at（调用时间）
- status（调用状态：成功/失败）

#### 3.5.3 cdb_aibridge_conversation 表
存储对话历史，字段包括：
- id（对话 ID）
- uid（用户 ID）
- platform（平台标识）
- messages_json（消息列表，JSON 格式）
- created_at（创建时间）
- updated_at（更新时间）

### 3.6 aibridge 插件文件结构

严格遵循 Discuz X5.0 插件规范，文件结构如下：

```
aibridge/
├── discuz_plugin_aibridge.json（插件描述文件）
├── install.php（安装脚本）
├── uninstall.php（卸载脚本）
├── enable.php（启用脚本）
├── disable.php（禁用脚本）
├── check.php（检查脚本）
├── upgrade.php（升级脚本）
├── hook.class.php（钩子处理类：plugin_aibridge、plugin_aibridge_forum）
├── admin.inc.php（后台主入口）
├── admin/
│   ├── admin_config.php（平台配置管理）
│   ├── admin_stats.php（用量统计）
│   └── admin_prompt.php（Prompt 模板管理）
├── table/
│   ├── table_aibridge_config.php（配置表操作类）
│   ├── table_aibridge_log.php（日志表操作类）
│   └── table_aibridge_conversation.php（对话表操作类）
├── lib/
│   ├── lib_aibridge.php（核心 AI 调用库）
│   ├── lib_openai.php（OpenAI 调用）
│   ├── lib_baidu.php（百度文心调用）
│   ├── lib_aliyun.php（阿里通义调用）
│   ├── lib_xunfei.php（讯飞星火调用）
│   ├── lib_tencent.php（腾讯混元调用）
│   ├── lib_deepseek.php（DeepSeek 调用）
│   ├── lib_claude.php（Anthropic Claude 调用）
│   └── lib_custom.php（自定义平台调用）
├── restful.class.php（RESTful API 接口）
├── cache/
│   └── cache_aibridge.php（缓存更新函数）
├── template/
│   ├── aibridge_chat.htm（AI 聊天悬浮窗模板）
│   └── aibridge_admin.htm（后台主模板）
├── static/
│   ├── aibridge.js（前台 JS）
│   └── aibridge.css（前台样式）
└── i18n/
    ├── SC_UTF8/
    │   └── lang_plugin.php（简体中文语言包）
    └── en/
        └── lang_plugin.php（英文语言包）
```

### 3.7 aibridge 插件开发规范要求

#### 3.7.1 代码安全规范
- 所有 PHP 文件必须以 `if(!defined('IN_DISCUZ')) { exit('Access Denied'); }` 作为安全检查
- API Key 使用 Discuz authcode 函数加密存储

#### 3.7.2 数据库操作规范
- 使用 Discuz 原生函数：`DB::fetch_all`、`DB::insert`、`DB::update` 等
- 表类继承 `discuz_table`，使用命名空间 `namespace aibridge;`

#### 3.7.3 后台界面规范
- 使用 Discuz 原生函数：`showformheader`、`showcomponent`、`showtableheader` 等

#### 3.7.4 API 调用规范
- 各平台 API 调用使用 PHP curl 实现
- 统一封装在 lib_aibridge.php 中
- 支持流式响应（SSE/Stream）可选配置

#### 3.7.5 前端开发规范
- 前台 AI 对话框采用原生 JS + CSS 实现
- 不依赖额外框架

### 3.8 演示网站页面结构与功能

#### 3.8.1 论坛前台

##### 3.8.1.1 论坛首页（路径：/）
- 顶部导航栏：
  - 论坛名称「技术论坛」
  - 快速发帖按钮
  - 登录状态显示（模拟已登录用户名）
- 分类板块列表：
  - 显示板块名称（如技术讨论、生活闲聊、问答求助）
  - 显示版主名称
  - 显示帖子数
  - 显示最新帖子标题和时间
- 右下角显示 AI 助手悬浮按钮

##### 3.8.1.2 板块页（路径：/forum/:id）
- 显示当前板块名称和描述
- 帖子列表：
  - 帖子标题
  - 作者名称
  - 回复数
  - 查看数
  - 最后回复时间
- 点击帖子标题跳转到帖子详情页

##### 3.8.1.3 帖子详情页（路径：/thread/:id）
- 显示帖子标题、作者、发布时间
- 显示帖子正文内容
- 显示回复列表（楼层编号、回复者、回复内容、回复时间）
- 帖子末尾显示「AI 智能回复建议」区块：
  - 「生成建议」按钮
  - 点击后显示 AI 生成的回复建议文本
  - 「使用此回复」按钮

#### 3.8.2 AI 助手悬浮窗（全局）
- 右下角浮动按钮，点击展开对话面板
- 对话面板包含：
  - AI 平台选择下拉框（显示 8 个平台名称）
  - Prompt 模板选择下拉框
  - 消息展示区域（用户消息和 AI 回复）
  - 输入框和发送按钮
  - 清空对话按钮
- 支持多轮对话（使用 Mock 数据模拟 AI 回复）

#### 3.8.3 aibridge 后台管理（路径：/admin/aibridge）

##### 3.8.3.1 后台布局
- 左侧导航栏：
  - 平台配置
  - Prompt 模板管理
  - 用量统计
- 右侧内容区域

##### 3.8.3.2 平台配置页（路径：/admin/aibridge/config）
- 表格展示 8 个 AI 平台：
  - 平台名称（OpenAI、百度文心、阿里通义、讯飞星火、腾讯混元、DeepSeek、Claude、自定义）
  - 启用状态开关
  - API Key（脱敏显示，如 sk-****abc123）
  - API 地址输入框
  - 模型选择下拉框
  - 额外配置 JSON 输入框
  - 操作按钮（保存）

##### 3.8.3.3 Prompt 模板管理页（路径：/admin/aibridge/prompt）
- 表格展示模板列表：
  - ID
  - 模板名称
  - 系统 Prompt 预览（截断显示）
  - 操作按钮（编辑、删除）
- 新增模板按钮，点击弹出弹窗：
  - 模板名称输入框
  - 系统 Prompt 文本域
  - 确认和取消按钮

##### 3.8.3.4 用量统计页（路径：/admin/aibridge/stats）
- 时间范围选择器（今日、本周、本月、全部）
- 总调用次数卡片
- 各平台调用次数表格
- Token 消耗统计表格
- 柱状图可视化展示各平台调用分布

#### 3.8.4 API 接口文档展示页（路径：/api-docs）
- 展示 3 个 RESTful 接口文档：
  - POST /api/aibridge/chat（AI 对话）
  - GET /api/aibridge/platforms（平台列表）
  - POST /api/aibridge/clear（清空对话）
- 每个接口包含：
  - 接口路径和请求方法
  - 请求参数说明（参数名、类型、必填、说明）
  - 响应格式说明
  - 请求和响应示例（JSON 格式）

### 3.9 演示网站 Mock 数据说明

#### 3.9.1 论坛数据
- 板块列表：包含 3-5 个板块（技术讨论、生活闲聊、问答求助等）
- 帖子列表：每个板块包含 10-20 条帖子
- 帖子详情：包含帖子正文和 5-10 条回复
- 用户信息：模拟已登录用户（用户名、头像）

#### 3.9.2 AI 平台配置数据
- 8 个平台的配置信息（平台名称、启用状态、脱敏 API Key、模型列表）

#### 3.9.3 Prompt 模板数据
- 3-5 个预设模板（如「技术助手」「内容润色」「翻译助手」）

#### 3.9.4 用量统计数据
- 总调用次数、各平台调用次数、Token 消耗数据

#### 3.9.5 AI 对话数据
- 用户发送消息后，返回预设的 Mock 回复内容
- 支持多轮对话历史记录

## 4. 业务规则与逻辑

### 4.1 aibridge 插件业务规则

#### 4.1.1 平台配置规则
- 每个平台的 API Key 使用 Discuz authcode 函数加密存储
- 自定义平台必须填写 API 地址和模型名称
- 至少启用一个平台才能使用前台功能
- 全局默认平台必须为已启用状态

#### 4.1.2 权限与频率限制
- 用户所属用户组未被授权时，无法使用 AI 功能
- 用户每日请求次数达到上限后，当日无法继续使用
- 单次请求 Token 数超过限制时，拒绝请求

#### 4.1.3 AI 调用流程
- 接收用户请求后，先验证权限和频率限制
- 根据指定平台调用对应的 lib 文件中的函数
- 调用成功后记录日志（uid、platform、model、tokens、时间、状态）
- 多轮对话时，从 conversation 表读取历史消息，拼接后发送给 AI
- 返回 AI 回复内容给前台

#### 4.1.4 对话历史管理
- 每个用户的对话独立存储
- 对话历史以 JSON 格式存储在 messages_json 字段
- 用户清空对话时，删除对应的 conversation 记录

#### 4.1.5 缓存机制
- 平台配置列表缓存，避免频繁查询数据库
- 配置更新时自动刷新缓存

#### 4.1.6 钩子注入规则
- common_header 钩子：在前台页面 header 中注入 AI 悬浮窗 HTML 和 JS
- post_jsoneditor_toolbar 钩子：在编辑器工具栏注入 AI 辅助按钮
- viewthread_display_end 钩子：在帖子末尾注入 AI 智能回复建议区域

### 4.2 演示网站业务规则

#### 4.2.1 路由规则
- 论坛首页：/
- 板块页：/forum/:id
- 帖子详情页：/thread/:id
- 后台管理首页：/admin/aibridge
- 平台配置页：/admin/aibridge/config
- Prompt 模板管理页：/admin/aibridge/prompt
- 用量统计页：/admin/aibridge/stats
- API 文档页：/api-docs

#### 4.2.2 Mock 数据交互规则
- 所有数据请求使用前端 Mock 数据响应
- AI 对话请求返回预设的 Mock 回复内容
- 配置保存操作仅更新前端状态，不发送后端请求
- 用量统计数据使用固定 Mock 数据展示

#### 4.2.3 UI 风格规则
- 采用 Discuz X5.0 经典论坛风格（蓝灰色调、表格化布局）
- 使用 Tailwind CSS 和 shadcn/ui 组件库
- 响应式设计，适配桌面和移动端

## 5. 异常与边界情况

| 场景 | 处理方式 |
|------|----------|
| API Key 未配置或错误 | 提示管理员检查配置，前台显示「AI 服务暂不可用」 |
| API 调用超时 | 提示用户「请求超时，请稍后重试」，记录失败日志 |
| API 返回错误 | 记录错误信息到日志，前台显示「AI 服务异常」 |
| 用户权限不足 | 提示「您没有使用 AI 功能的权限」 |
| 达到频率限制 | 提示「今日使用次数已达上限，请明天再试」 |
| Token 数超限 | 提示「输入内容过长，请精简后重试」 |
| 未启用任何平台 | 后台提示管理员至少启用一个平台 |
| 对话历史过长 | 自动截断早期消息，保留最近 N 轮对话 |
| 网络异常 | 提示用户「网络连接失败，请检查网络」 |
| 演示网站路由不存在 | 显示 404 页面 |
| Mock 数据加载失败 | 显示「数据加载失败」提示 |

## 6. 验收标准

1. 管理员在 aibridge 插件后台成功配置至少一个 AI 平台（填写 API Key、选择模型、启用平台）
2. 管理员设置用户组权限，允许指定用户组使用 AI 功能
3. 授权用户在前台点击悬浮按钮，打开 AI 对话框，输入消息后收到 AI 回复
4. 用户在发帖编辑器中选中文本，点击 AI 按钮选择「润色」，编辑器中显示 AI 润色后的内容
5. 用户查看帖子时，点击「生成建议」按钮，AI 回复建议区域显示智能回复内容
6. 管理员在用量统计页面查看总调用次数和各平台调用分布数据
7. 用户通过浏览器访问 http://127.0.0.1:8082 进入演示网站论坛首页，查看板块列表
8. 用户点击板块进入板块页，查看帖子列表，点击帖子标题进入帖子详情页
9. 用户在帖子详情页点击「生成建议」按钮，查看 AI 智能回复建议
10. 用户点击右下角 AI 助手按钮，展开对话面板，选择平台和模板，发送消息后收到 Mock 回复
11. 用户访问 /admin/aibridge/config 查看平台配置列表，修改配置并保存
12. 用户访问 /admin/aibridge/prompt 查看 Prompt 模板列表，新增、编辑或删除模板
13. 用户访问 /admin/aibridge/stats 查看用量统计数据和柱状图
14. 用户访问 /api-docs 查看 3 个 RESTful 接口文档

## 7. 本期不实现功能

- AI 语音对话功能
- AI 图片生成功能
- AI 内容审核功能
- 用户自定义 AI 参数（temperature、top_p 等）
- AI 对话导出功能
- 多语言自动翻译（仅支持手动选择翻译）
- AI 模型自动切换（根据负载或成本）
- 用户级别的 API Key 配置
- AI 对话分享功能
- 移动端独立 UI 适配
- 演示网站真实后端接口对接
- 演示网站用户注册和登录功能
- 演示网站真实发帖和回复功能
- 演示网站数据持久化存储